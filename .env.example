DATABASE_URL="postgresql://splitwise:splitwise@localhost:5432/splitwise"
AUTH_SECRET="your-secret-at-least-32-characters-long"
AUTH_URL="http://localhost:3000"
# Browser HTTP boundary. Keep same-origin in production and switch its upstream at the edge.
NEXT_PUBLIC_API_BASE_URL="/api/v1"
# Absolute server-side backend base (including /api/v1). Defaults to AUTH_URL + /api/v1.
# BACKEND_API_BASE_URL="http://localhost:3000/api/v1"
# TEST_DATABASE_URL may override the automatically derived <database>_test URL.
